package by.epam.java_training.mihail_poliansky.final_project.service.validator.strategy;

public class TimeManagerItemValidator implements Validator {
    @Override
    public <T> boolean validate(T t) {
        return true;
    }
}
